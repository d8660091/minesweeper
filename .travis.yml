sudo: no
dist: trusty
language: python
python:
  - "3.6"
env:
  - TOXENV=py36-tests FOO=foo
  - TOXENV=py36-flake8 FOO=foo
install:
  - pip install coveralls tox
script:
  - tox
after_success: coveralls
